import {people} from '@/data/people';
export function generateStaticParams(){return Object.keys(people).map(person=>({person}))}
export async function GET(_request:Request,{params}:{params:Promise<{person:string}>}){const {person}=await params;const p=people[person];if(!p)return new Response('Not found',{status:404});const esc=(s:string)=>s.replace(/\\/g,'\\\\').replace(/\n/g,'\\n').replace(/,/g,'\\,').replace(/;/g,'\\;');const lines=['BEGIN:VCARD','VERSION:3.0',`FN:${esc(p.name)}`,`N:${esc(p.name.split(' ').slice(1).join(' '))};${esc(p.name.split(' ')[0])};;;`,'ORG:Horyzon Consulting',`TITLE:${esc(p.role)}`,...(p.phone?[`TEL;TYPE=CELL:${p.phone}`]:[]),...(p.email?[`EMAIL;TYPE=WORK:${p.email}`]:[]),`URL:https://horyzon.it/${person}/`,'END:VCARD',''];return new Response(lines.join('\r\n'),{headers:{'Content-Type':'text/vcard; charset=utf-8','Content-Disposition':`attachment; filename="${person}.vcf"`}})}
