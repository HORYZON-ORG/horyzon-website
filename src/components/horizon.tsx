"use client";
import { motion, useScroll, useTransform, useReducedMotion } from 'motion/react';
import { useRef, useState } from 'react';
import Link from 'next/link';
const steps=[{name:'Ascolto',title:'Prima le domande giuste.',text:'Persone, attriti e aspirazioni. Partiamo da ciò che vivi ogni giorno.',labels:['Persone','Tempo','Obiettivi']},{name:'Misura',title:'Rendi visibile la priorità.',text:'Diamo un ordine ai segnali. Scegliamo dove intervenire e cosa osservare.',labels:['Priorità','Dati','Chiarezza']},{name:'Azione',title:'Il cambiamento prende forma.',text:'Responsabilità chiare, competenze mirate. Un passo concreto da verificare insieme.',labels:['Competenze','Risultati','Libertà']}];
export function Horizon(){const ref=useRef<HTMLDivElement>(null);const reduce=useReducedMotion();const {scrollYProgress}=useScroll({target:ref,offset:['start start','end start']});const y=useTransform(scrollYProgress,[0,1],['0%','25%']);return <div ref={ref} className="horizon-scene" aria-hidden="true"><motion.div className="horizon-sun" style={reduce?{}:{y}}/><div className="horizon-line"/><div className="horizon-water"/><div className="horizon-coordinates"><span>VISIONE / 01</span><span>VERSO IL BENESSERE</span></div></div>}
export function MethodExperience(){const [active,setActive]=useState(0);return <section className="method-experience" id="metodo"><div className="method-intro"><p className="eyebrow"><span/> Il metodo Horyzon</p><h2>Dal rumore<br/><em>all’orizzonte.</em></h2><p>Un problema alla volta.<br/>Una direzione che tiene insieme tutto.</p><Link className="text-link" href="/metodo">Esplora il metodo ↗</Link></div><div className={`method-console phase-${active}`}><div className="method-field" aria-hidden="true"><div className="method-axis"/><div className="method-sun"/>{steps[active].labels.map((label,i)=><span className={`method-label label-${i}`} key={label}>{label}</span>)}<span className="field-caption">{active===0?'SEGNALI DA ASCOLTARE':active===1?'UNA PRIORITÀ EMERGE':'UNA DIREZIONE CONDIVISA'}</span></div><div className="method-controls" aria-label="Passaggi del metodo">{steps.map((s,i)=><button key={s.name} aria-pressed={active===i} onClick={()=>setActive(i)} onMouseEnter={()=>setActive(i)} onFocus={()=>setActive(i)}><span>0{i+1}</span>{s.name}</button>)}</div><div className="method-detail" aria-live="polite"><h3>{steps[active].title}</h3><p>{steps[active].text}</p></div></div></section>}
