import { departments } from '@/content/site-narrative';
export function BusinessOrganism({ compact = false }: { compact?: boolean }): React.JSX.Element { return <div className={`business-organism ${compact ? 'is-compact' : ''}`}><div className="organism-core"><span>Direzione condivisa</span><strong>Impresa</strong></div><div className="organism-departments">{departments.map((department,index)=><details key={department.id} open={!compact && index===0}><summary><span>{String(index+1).padStart(2,'0')}</span>{department.name}</summary><div className="department-detail"><p>{department.purpose}</p><h3>Segnali da osservare</h3><ul>{department.signals.map(signal=><li key={signal}>{signal}</li>)}</ul><p><strong>Direzione:</strong> {department.destination}</p></div></details>)}</div></div>; }
